apiVersion: cluster.argocd.crossplane.io/v1alpha1
kind: Cluster
metadata:
  name: rpi
  namespace: argocd
spec:
  forProvider:
    labels:
      deploy-cert-manager: "true"
      deploy-cilium: "true"
      deploy-metrics-server: "true"
      deploy-kube-prometheus-stack: "true"
      deploy-grafana-operator: "true"
      addon/cert-manager: "true"
      addon/cilium: "true"
      addon/metrics-server: "true"
      addon/kube-prometheus-stack: "true"
      addon/grafana-operator: "true"
      cluster-type: "management"
    config:
      tlsClientConfig:
        insecure: true
    name: rpi
    server: https://kubernetes.default.svc
  providerConfigRef:
    name: argocd-provider
