core:
  cluster-api: {}
infrastructure:
  vcluster: {}
  proxmox:
    manifestPatches: |
      apiVersion: apps/v1
      kind: Deployment
      spec:
        template:
          spec:
            containers:
              - name: manager
                imageUrl: ghcr.io/jjsiv/cluster-api-provider-proxmox:v0.7.5
ipam:
  in-cluster: {}
